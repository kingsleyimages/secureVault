function vault() {
  // create message variable and assign string to it.
  let message =
    'You have received this message because you have been chosen to open an important vault. Here is the secret combination:';
  // create three variables and assign calculations
  const num1 = 2000 / 200;
  const num2 = 2024 - 1984;
  const num3 = 2 * 4 * 6 + 1;
  // output alert that contains the message and combination to the vault
  alert(message + '\n\n' + num1 + ',' + num2 + ',' + num3);
  console.log(message + '\n\n' + num1 + ',' + num2 + ',' + num3);
}
vault();
